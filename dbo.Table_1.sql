CREATE TABLE [dbo].[Table] (
    [PId]   INT          NOT NULL,
    [PName] VARCHAR (50) NOT NULL,
    [PType] INT          NOT NULL,
    [Price] INT          NOT NULL,
    PRIMARY KEY CLUSTERED ([PId] ASC)
);

